<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "PRUEBARAILS", root_path, class: "navbar-brand" %>  
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

   <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to  "Salir", destroy_user_session_path, method: :delete, class: "nav-link fixed-right" %>
        </li>  
        <% else %>
        <li class="nav-item">
          <%= link_to  "Log_in", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to  "Registrate", new_user_registration_path, class: "nav-link" %>
        </li>
        <% end %>
      <li class="nav-item">
       <span class="nav-link"> <%= current_user.email if user_signed_in? %> </span>
      </li>
      <li class="nav-item text-white mx-5 my-2">
      <%if user_signed_in?%>
      <%= current_user.todos.count {|todo| todo.completed == true} %> de <%= current_user.todos.count    %> completadas 
  
      
      <%end%>
      </li>
    </ul>
  </div>
</nav>